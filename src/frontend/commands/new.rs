use crate::frontend::context::Context;

pub fn run_command(ctx: &mut Context) {
  /*
  let name = ctx.cwd.clone().push(expect_some!(ctx.args.first(), f!({"Usage".bold()}": vega new {name}")).into());
  let data = toml! {
    [project]
    name = name
    version = "0.0.1"
    vega = "0.0.1"
    [dependencies]
  }
  write_file(name, data.to_string())
  */
}
